# Product workflow GIFs (landing “See the workflow”)

Record **three ~7-second looping GIFs** (Screenity / CleanShot X / Kap) from the **real** TapeOutOps UI and place them here:

| File | Scene to record |
|------|-----------------|
| `speclint-upload.gif` | Upload a spec → SpecLint console flags an out-of-bounds / rule error |
| `checklist-approve.gif` | EM clicks **Approve** on a DRC/LVS gate checklist → audit history updates |
| `vendor-handoff.gif` | Open Vendor Performance View with siloed foundry/OSAT staging |

Until these files exist, the landing page shows CSS fallbacks automatically.

Also add `../og-share.png` (1200×630) for LinkedIn / WhatsApp link previews (referenced from `index.html`).
