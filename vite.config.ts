/**
 * Vite resolves `vite.config.mjs` for this project. This file re-exports it so any
 * tooling that only looks for `vite.config.ts` still sees the same config.
 */
export { default } from './vite.config.mjs'
